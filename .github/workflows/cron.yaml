name: sunday-cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Telegram API route
        run: |
          curl --request GET \
          --url 'telegram-hwd4vhsgn-bookcliff.vercel.app/api/bot'